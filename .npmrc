# react-helmet-async@2.0.5 declares a peer dependency on React <=18, but this project
# runs React 19. It works correctly at runtime — the constraint is just stale upstream.
# Without this flag, `npm install` and `npm ci` both fail on a clean checkout (including CI).
legacy-peer-deps=true
